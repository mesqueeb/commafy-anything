{
  "name": "commafy-anything",
  "version": "4.0.3",
  "description": "Adds comma's to a number. A simple and small integration",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit && eslint ./src",
    "build": "del-cli dist && tsc",
    "release": "pnpm run lint && pnpm run build && pnpm test && bumpp --no-push && pnpm publish --no-git-checks && git push --follow-tags && gh release create $(git describe --tags --abbrev=0) --generate-notes --notes \"$(git log --pretty='- %s  %h' $(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD~1)\""
  },
  "devDependencies": {
    "@cycraft/eslint": "^0.4.10",
    "@cycraft/tsconfig": "^0.1.2",
    "bumpp": "^12.3.0",
    "del-cli": "^7.0.0",
    "vitest": "^5.0.1"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "comma",
    "comma's",
    "commafy",
    "number"
  ],
  "author": "Luca Ban - Mesqueeb (https://cycraft.co)",
  "funding": "https://github.com/sponsors/mesqueeb",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mesqueeb/commafy-anything.git"
  },
  "homepage": "https://github.com/mesqueeb/commafy-anything#readme",
  "bugs": "https://github.com/mesqueeb/commafy-anything/issues",
  "packageManager": "pnpm@12.4.2"
}
